---
phase: 01-core-orchestration-branding
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - commands/grd/*.md
  - agents/grd-*.md
  - get-research-done/**/*.md
autonomous: true

must_haves:
  truths:
    - "All command frontmatter uses 'name: grd:*' pattern"
    - "All agent frontmatter uses 'name: grd-*' pattern"
    - "All @file references use get-research-done/ paths"
    - "All workflow/template references use grd: command prefix"
  artifacts:
    - path: "commands/grd/new-project.md"
      provides: "Renamed new-project command"
      contains: "name: grd:new-project"
    - path: "agents/grd-executor.md"
      provides: "Renamed executor agent"
      contains: "name: grd-executor"
    - path: "get-research-done/workflows/execute-plan.md"
      provides: "Renamed workflow file"
      contains: "grd:"
  key_links:
    - from: "commands/grd/*.md"
      to: "agents/grd-*.md"
      via: "agent spawn references"
      pattern: "grd-executor|grd-planner"
    - from: "get-research-done/workflows/*.md"
      to: "get-research-done/templates/*.md"
      via: "@file references"
      pattern: "@.*get-research-done"
---

<objective>
Update all text references from GSD to GRD across commands, agents, and workflows

Purpose: After Plan 01 renamed the directories and files, this plan updates all the text content inside those files to use the new GRD naming convention. This ensures commands work when invoked with /grd: prefix.

Output: All ~70 markdown files updated with grd: command names, grd- agent names, and get-research-done/ path references
</objective>

<execution_context>
@/Users/evanowen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evanowen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-orchestration-branding/01-RESEARCH.md
@.planning/phases/01-core-orchestration-branding/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update command file frontmatter and content</name>
  <files>commands/grd/*.md (27 files)</files>
  <action>
Update all command files in commands/grd/ directory:

1. Update frontmatter `name:` field from `gsd:*` to `grd:*`:
   ```bash
   # For each command file, update the name field
   find commands/grd -name "*.md" -exec sed -i '' 's/^name: gsd:/name: grd:/g' {} +
   ```

2. Update any references to gsd: commands within file content:
   ```bash
   find commands/grd -name "*.md" -exec sed -i '' 's/gsd:/grd:/g' {} +
   ```

3. Update agent references from gsd-* to grd-*:
   ```bash
   find commands/grd -name "*.md" -exec sed -i '' 's/gsd-/grd-/g' {} +
   ```

4. Update path references from get-shit-done to get-research-done:
   ```bash
   find commands/grd -name "*.md" -exec sed -i '' 's/get-shit-done/get-research-done/g' {} +
   ```

5. Verify each command file has correct frontmatter:
   ```bash
   for f in commands/grd/*.md; do
     head -5 "$f" | grep -q "name: grd:" && echo "OK: $f" || echo "FAIL: $f"
   done
   ```
  </action>
  <verify>
    - `grep -r "name: gsd:" commands/grd/` returns no results
    - `grep -r "name: grd:" commands/grd/ | wc -l` returns 27
    - `grep -r "get-shit-done" commands/grd/` returns no results
    - `grep -r "gsd-" commands/grd/` returns no results (agent references updated)
  </verify>
  <done>
    All 27 command files have grd: naming in frontmatter and content
  </done>
</task>

<task type="auto">
  <name>Task 2: Update agent file frontmatter and content</name>
  <files>agents/grd-*.md (11 files)</files>
  <action>
Update all agent files in agents/ directory:

1. Update frontmatter `name:` field from `gsd-*` to `grd-*`:
   ```bash
   find agents -name "grd-*.md" -exec sed -i '' 's/^name: gsd-/name: grd-/g' {} +
   ```

2. Update any internal references to gsd: commands:
   ```bash
   find agents -name "grd-*.md" -exec sed -i '' 's/gsd:/grd:/g' {} +
   ```

3. Update cross-agent references from gsd-* to grd-*:
   ```bash
   find agents -name "grd-*.md" -exec sed -i '' 's/gsd-/grd-/g' {} +
   ```

4. Update path references:
   ```bash
   find agents -name "grd-*.md" -exec sed -i '' 's/get-shit-done/get-research-done/g' {} +
   ```

5. Verify agent frontmatter:
   ```bash
   for f in agents/grd-*.md; do
     head -5 "$f" | grep -q "name: grd-" && echo "OK: $f" || echo "FAIL: $f"
   done
   ```
  </action>
  <verify>
    - `grep -r "name: gsd-" agents/` returns no results
    - `grep -r "name: grd-" agents/grd-*.md | wc -l` returns 11
    - `grep -r "get-shit-done" agents/` returns no results
  </verify>
  <done>
    All 11 agent files have grd- naming in frontmatter and updated references
  </done>
</task>

<task type="auto">
  <name>Task 3: Update workflow and template files</name>
  <files>get-research-done/**/*.md (~51 files)</files>
  <action>
Update all files in get-research-done/ directory tree:

1. Update gsd: command references to grd:
   ```bash
   find get-research-done -name "*.md" -exec sed -i '' 's/gsd:/grd:/g' {} +
   ```

2. Update gsd- agent references to grd-:
   ```bash
   find get-research-done -name "*.md" -exec sed -i '' 's/gsd-/grd-/g' {} +
   ```

3. Update path references:
   ```bash
   find get-research-done -name "*.md" -exec sed -i '' 's/get-shit-done/get-research-done/g' {} +
   ```

4. Update any GSD text mentions to GRD:
   ```bash
   find get-research-done -name "*.md" -exec sed -i '' 's/GSD/GRD/g' {} +
   ```

5. Count updated files:
   ```bash
   find get-research-done -name "*.md" | wc -l
   ```

6. Stage and commit all changes:
   ```bash
   git add commands/grd/ agents/grd-*.md get-research-done/
   git commit -m "refactor(01-03): update all text references from GSD to GRD

   - Update 27 command files: name: gsd:* → name: grd:*
   - Update 11 agent files: name: gsd-* → name: grd-*
   - Update ~51 workflow/template files
   - Replace all get-shit-done paths with get-research-done
   - Replace all gsd: prefixes with grd:
   - Replace all gsd- prefixes with grd-

   Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>"
   ```
  </action>
  <verify>
    - `grep -r "get-shit-done" get-research-done/` returns no results
    - `grep -r "gsd:" get-research-done/` returns no results
    - `grep -r "gsd-" get-research-done/` returns no results
    - `grep -r "GSD" get-research-done/` returns no results (or only in changelog/history context)
  </verify>
  <done>
    All workflow and template files updated with GRD naming
  </done>
</task>

</tasks>

<verification>
Run comprehensive verification:

```bash
# Check for any remaining GSD references across all renamed areas
echo "=== Checking for remaining GSD references ==="

echo "Commands (should be 0):"
grep -r "gsd:\|gsd-\|get-shit-done" commands/grd/ 2>/dev/null | wc -l

echo "Agents (should be 0):"
grep -r "gsd:\|gsd-\|get-shit-done" agents/grd-*.md 2>/dev/null | wc -l

echo "Workflows/Templates (should be 0):"
grep -r "gsd:\|gsd-\|get-shit-done" get-research-done/ 2>/dev/null | wc -l

# Check for correct GRD references
echo ""
echo "=== Verifying GRD references ==="

echo "Command names (should be 27):"
grep -r "^name: grd:" commands/grd/ | wc -l

echo "Agent names (should be 11):"
grep -r "^name: grd-" agents/grd-*.md | wc -l
```
</verification>

<success_criteria>
- 27 command files have `name: grd:*` in frontmatter
- 11 agent files have `name: grd-*` in frontmatter
- Zero references to `gsd:`, `gsd-`, or `get-shit-done` remain in commands, agents, or workflows
- All @file references use `get-research-done/` paths
- Changes committed as single atomic operation
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-orchestration-branding/01-03-SUMMARY.md`
</output>
